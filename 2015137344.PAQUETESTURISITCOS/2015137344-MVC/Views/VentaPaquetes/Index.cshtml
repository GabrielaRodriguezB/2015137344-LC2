@model IEnumerable<_2015137344_ENT.Entities.VentaPaquete>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MedioPago.NomMedioPago)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MontoAPagar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComprobantePagoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MedioPago.NomMedioPago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MontoAPagar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComprobantePagoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VentaPaqueteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VentaPaqueteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VentaPaqueteId })
        </td>
    </tr>
}

</table>

@model IEnumerable<_2015137344_ENT.Entities.Paquete>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomPaquete)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecioPaquete)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomPaquete)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecioPaquete)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaqueteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaqueteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaqueteId })
        </td>
    </tr>
}

</table>
